version: '3.8'

services:

  website1:
    build:
      context: ./website1
    container_name: website1
    ports:
      - "8081:80"
    restart: always

  website2:
    build:
      context: ./website2
    container_name: website2
    ports:
      - "8082:80"
    restart: always
